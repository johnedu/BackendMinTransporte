<div class="modal-header">
    <h2>@L("")Eliminar Categoría</h2>
</div>
<div class="modal-body">
    <div class="alert alert-danger text-center" ng-show="mensajeError">
        <strong>{{mensajeError}}</strong>
    </div>
    <div>
        <p>@L("")¿Está seguro que desea eliminar la siguiente categoría?</p>
        <ul>
            <li><strong>{{categoria.nombre}}</strong></li>
        </ul>
    </div>
</div>
<div class="modal-footer">
    <button class="btn btn-success" ng-click="okModal()">@L("")Aceptar</button>
    <button class="btn btn-danger" ng-click="cancelModal()">@L("")Cancelar</button>
</div>